 <%= form_with(model: feature, local: true) do |form| %>
  <% if feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feature.errors.count, "error") %> prohibited this feature from being saved:</h2>

      <ul>
      <% feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group new_user">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group new_user">
    <div class="field">
      <%= form.label :code %>
      <%= form.text_field :code, class: "form-control" %>
    </div>
  </div>

  <div class="form-group new_user">
    <div class="field">
      <%= form.label :unit_price %>
      <%= form.text_field :unit_price, class: "form-control" %>
    </div>
  </div>

  <div class="form-group new_user">
    <div class="field">
      <%= form.label :max_unit_limit %>
      <%= form.text_field :max_unit_limit, class: "form-control" %>
    </div>
  </div>


  <div class="actions new_user">
    <%= form.submit "Update feature", class:"btn btn-primary"%>
  </div>
<% end %>
